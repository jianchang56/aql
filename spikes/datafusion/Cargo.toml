[package]
name = "aql-datafusion-spike"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
async-trait = "0.1"
datafusion = { version = "54.0.0", default-features = false, features = ["sql"] }
serde_json = "1.0"
tokio = { version = "1.0", features = ["macros", "rt-multi-thread", "time"] }
